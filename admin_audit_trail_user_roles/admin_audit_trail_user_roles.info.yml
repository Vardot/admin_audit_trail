name: "Admin Audit Trail User Roles"
description: "Logs user role assignment and removal events."
type: module
core_version_requirement: ^10.1 || ^11 || ^12
package: Administration
dependencies:
  - admin_audit_trail:admin_audit_trail
