name: "Admin Audit Trail Logger"
description: "Forwards Admin Audit Trail events to the PSR-3 logger (syslog, Monolog, etc.)."
type: module
core_version_requirement: ^10.1 || ^11 || ^12
package: Administration
configure: admin_audit_trail_logger.settings
dependencies:
  - admin_audit_trail:admin_audit_trail
